\chapter{Introduzione e Architettura dell'Information Retrieval}

\FloatBarrier
\section{Definizione e Motivazioni}

L'\textbf{Information Retrieval} (IR, \textit{Reperimento delle Informazioni}) \`e la disciplina scientifica e ingegneristica che studia la rappresentazione, la memorizzazione, l'organizzazione e l'accesso a collezioni di dati prevalentemente non strutturati o semi-strutturati, al fine di soddisfare un \textbf{bisogno informativo} (\textit{information need}) espresso da un utente~\cite{manning2008,buttcher2010}.

\begin{definition}[Information Retrieval (Manning et al.)]
L'Information Retrieval consiste nel reperire materiale di natura prevalentemente non strutturata (solitamente documenti testuali) che soddisfa un bisogno informativo all'interno di collezioni di grandi dimensioni memorizzate su calcolatori elettronici.
\end{definition}

Nell'ecosistema tecnologico contemporaneo, l'Information Retrieval non si limita ai soli motori di ricerca Web generalisti (come Google o Bing), ma costituisce l'infrastruttura abilitante di:
\begin{itemize}
    \item \textbf{E-commerce e marketplace:} reperimento di prodotti basato su descrizioni testuali, attributi e recensioni degli acquirenti (es. Amazon, Alibaba, eBay).
    \item \textbf{Enterprise e Desktop Search:} indicizzazione e ricerca semantica su repository aziendali (documenti, email, codebase sorgente).
    \item \textbf{Sistemi RAG (\textit{Retrieval-Augmented Generation}):} alimentazione di modelli linguistici di grandi dimensioni (LLM) con documenti esterni contestualmente rilevanti per azzerare le allucinazioni e garantire informazioni aggiornate in tempo reale.
    \item \textbf{Agenti autonomi e motori di raccomandazione:} filtraggio collaborativo e basato su contenuto per la scoperta personalizzata di contenuti e streaming multimediale (es. Netflix, Spotify, Meta, Pinterest).
\end{itemize}

\subsection{L'Evoluzione nell'Era Generativa: dai Motori Web all'Agentic Search}

L'avvento dei modelli linguistici conversazionali (LLM) non ha reso obsoleto l'Information Retrieval; al contrario, ne ha ampliato radicalmente l'impatto. Un'intervista ideale a un moderno LLM evidenzia come esso stesso operi in stretta sinergia con i principi cardine dell'IR:
\begin{enumerate}
    \item \textbf{Comprensione Semantica della Query:} elaborazione avanzata del linguaggio naturale (NLP) per disambiguare l'intento informativo sottostante.
    \item \textbf{Matching e Ranking:} selezione e pesatura delle informazioni pi\`u rilevanti all'interno di sterminati archivi di conoscenza.
    \item \textbf{Reperimento Contestuale (\textit{Fresh Information}):} un modello generativo puro \`e limitato dal momento del suo addestramento (\textit{knowledge cutoff}) e non pu\`o rispondere a quesiti su eventi recenti o dati privati senza riaddestrare la rete neurale (operazione economicamente e computazionalmente proibitiva).
\end{enumerate}

Per superare questo limite, i moderni assistenti adottano il paradigma \textbf{RAG (\textit{Retrieval-Augmented Generation})}: il sistema intercetta la richiesta dell'utente, interroga un motore IR via Web o su database documentale vettoriale, inietta i documenti estratti all'interno del contesto dell'LLM e genera una risposta argomentata e supportata da citazioni verificabili.

\subsubsection{Agentic Search e il Ciclo Adattivo a Feedback}
Mentre il RAG tradizionale adotta una pipeline lineare a singolo passaggio (\textit{Single-hop}: query $\to$ retrieval $\to$ generation), i problemi complessi richiedono il paradigma emergente della \textbf{Agentic Search} (Ricerca Agentica). 

\begin{definition}[Agentic Search]
L'\textbf{Agentic Search} \`e un processo di reperimento dinamico in cui un agente intelligente determina in modo autonomo e condizionato quali ricerche successive condurre, basandosi sulle evidenze parziali raccolte negli stadi precedenti. L'agente ispeziona le fonti, individua discrepanze o lacune conoscitive (\textit{information gaps}), formula nuove query di raffinamento e reitera il ciclo fino alla convergenza informativa.
\end{definition}

\begin{figure}[htbp]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    stepbox/.style={rectangle, rounded corners=4pt, draw=blue!80!black, fill=blue!8, thick, minimum width=2.8cm, minimum height=1.0cm, align=center, font=\small\bfseries},
    evalbox/.style={rectangle, rounded corners=4pt, draw=orange!80!black, fill=orange!12, thick, minimum width=2.8cm, minimum height=1.0cm, align=center, font=\small\bfseries},
    outbox/.style={rectangle, rounded corners=4pt, draw=teal!80!black, fill=teal!12, thick, minimum width=2.8cm, minimum height=1.0cm, align=center, font=\small\bfseries},
    arr/.style={-Stealth, thick, blue!85!black},
    looparr/.style={-Stealth, thick, dashed, red!75!black}
]
    \node[stepbox] (search)  at ( 0.0, 0) {1. Search\\[3pt]\scriptsize (Esecuzione query)};
    \node[stepbox] (inspect) at ( 5.5, 0) {2. Inspect\\[3pt]\scriptsize (Analisi evidenze)};
    \node[evalbox] (gaps)    at (11.0, 0) {3. Identify Gaps\\[3pt]\scriptsize (Rilevazione lacune)};
    \node[outbox]  (answer)  at (16.5, 0) {4. Synthesize\\[3pt]\scriptsize (Risposta finale)};

    \draw[arr] (search)  -- (inspect);
    \draw[arr] (inspect) -- (gaps);
    % Freccia curva che passa sopra i box per evitare sovrapposizioni con la label
    \draw[arr, bend left=40] (gaps.north) to node[above, font=\small\bfseries, fill=white, inner sep=3pt, text=teal!70!black] {Completo} (answer.north);
    \draw[looparr] (gaps.south) -- ++(0,-1.4) -| node[pos=0.25, below, font=\small\bfseries, text=red!75!black, fill=white, inner sep=3pt] {Lacune informative rilevate $\rightarrow$ Nuova query mirata} (search.south);
\end{tikzpicture}%
}
\caption{Ciclo a retroazione adattivo (\textit{Adaptive Feedback Loop}) dell'Agentic Search.}
\label{fig:agentic-search-loop}
\end{figure}


Si consideri ad esempio il quesito: \textit{"Per quale motivo gli utili della societ\`a X sono scesi lo scorso trimestre nonostante l'aumento del fatturato?"}. Un sistema agentico esegue una prima ricerca per reperire il report trimestrale, constata la discrepanza tra ricavi e margini operativi, identifica la necessit\`a di investigare l'andamento dei costi operativi o delle materie prime, formula una seconda query mirata su tali fattori e unisce infine le risultanze in un'argomentazione organica e referenziata.

\subsection{Information Retrieval vs. Data Retrieval (Basi di Dati)}
Una distinzione metodologica basilare risiede nella contrapposizione tra il modello convenzionale dei database relazionali (\textit{Data Retrieval}) e l'Information Retrieval:

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{>{\raggedright\bfseries\arraybackslash}p{4.2cm} >{\raggedright\arraybackslash}X >{\raggedright\arraybackslash}X}
\toprule
\normalfont\textbf{Dimensione} & \textbf{Data Retrieval (Database SQL)} & \textbf{Information Retrieval} \\
\midrule
Struttura dei Dati & Dati altamente strutturati con schema relazionale rigido (tabelle, tipi, vincoli di integrit\`a) & Dati non strutturati o semi-strutturati (testo libero, HTML, PDF, metadati) \\
\addlinespace
Linguaggio di Query & Query formali e deterministiche (ad es.\ SQL) con semantica algebrica esatta & Query in linguaggio naturale o parole chiave (spesso ambigue, brevi e incomplete) \\
\addlinespace
Criterio di Matching & Corrispondenza booleana esatta: un record appartiene o non appartiene al risultato & Corrispondenza parziale e probabilistica: i documenti presentano gradi variabili di rilevanza \\
\addlinespace
Ordinamento Risultati & Nessun ordinamento intrinseco (se non specificato con \texttt{ORDER BY}) & Ordinamento fondamentale basato sulla stima di rilevanza (\textit{Relevance Ranking}) \\
\addlinespace
Tolleranza all'Errore & Nulla: errori di sintassi o valori inattesi generano errori o insiemi vuoti & Elevata: tolleranza a refusi, sinonimia, polisemia e variabilit\`a morfologica \\
\bottomrule
\end{tabularx}
\caption{Confronto analitico tra Data Retrieval tradizionale e Information Retrieval.}
\label{tab:ir-vs-dr}
\end{table}


\begin{alertblock}{La Rilevanza: Il Cardine dell'IR}
Mentre in una base di dati un record soddisfa o viola in modo booleano un predicato logico, nell'Information Retrieval il fulcro \`e la stima del grado di \textbf{rilevanza} rispetto al bisogno informativo cognitivo dell'utente. La rilevanza \`e soggettiva, situata nel contesto operativo ed evolutiva nel tempo.
\end{alertblock}

\FloatBarrier
\section{I Due Macro-Obiettivi: Efficacia ed Efficienza}

Un sistema di reperimento dell'informazione su larga scala deve simultaneamente perseguire due obiettivi spesso in contrasto:
\begin{enumerate}
    \item \textbf{Efficacia (\textit{Effectiveness}):} Quantifica la qualit\`a intrinseca dei risultati restituiti, misurando quanto accuratamente il ranking soddisfa il bisogno informativo dell'utente (argomento approfondito nel Capitolo~\ref{chap:evaluation}).
    \item \textbf{Efficienza (\textit{Efficiency}):} Quantifica il costo computazionale, il throughput (query al secondo) e la latenza di risposta (generalmente richiesta nell'ordine dei $10$--$50$ millisecondi per query su archivi di decine di miliardi di documenti).
\end{enumerate}

\subsection{Efficienza e Architettura Hardware del Calcolatore}
L'efficienza di un motore di ricerca moderno non dipende unicamente dalla complessit\`a asintotica degli algoritmi, ma dall'interazione profonda con l'architettura dei processori e della memoria:
\begin{itemize}
    \item \textbf{Pipelining e Processori Superscalari:} I moderni processori suddividono l'esecuzione delle istruzioni in stadi sequenziali (Instruction Fetch, Decode, Execute, Memory Access, Write Back) ed eseguono molteplici istruzioni per ciclo di clock.
    \item \textbf{Pipeline Hazards (Rischi di Pipeline):}
    \begin{itemize}
        \item \textit{Structural Hazards:} Conflitti nell'uso simultaneo di risorse hardware limitate (ad es. unit\`a di memoria o registri).
        \item \textit{Data Hazards:} Dipendenze logiche tra istruzioni consecutive (RAW: Read-After-Write), che costringono il processore a inserire cicli di stallo (\textit{pipeline bubble}) se i dati non sono pronti.
        \item \textit{Control Hazards:} Salto condizionale non previsto. Nella scansione e nell'intersezione delle posting list, un'istruzione condizionale errata (\textit{branch misprediction}) svuota la pipeline, comportando una penalit\`a di $15$--$20$ cicli di clock per ogni errore di predizione.
    \end{itemize}
    \item \textbf{Gerarchia di Memoria (\textit{Memory Hierarchy}):} Il tempo di accesso ai registri della CPU \`e sub-nanosecondo, alle cache L1/L2/L3 \`e di pochi cicli, mentre l'accesso alla RAM DDR richiede $50$--$100$ ns e lo storage secondario (NVMe/SSD) richiede decine di microsecondi. La progettazione delle posting list privilegia layout ad accessi sequenziali contigui per massimizzare il riuso delle linee di cache e sfruttare l'hardware prefetcher.
    \item \textbf{Vettorizzazione SIMD (\textit{Single Instruction Multiple Data}):} L'impiego di registri vettoriali a 128, 256 o 512 bit (AVX-2, AVX-512, ARM NEON) consente di confrontare 8 o 16 identificatori di documento con una singola istruzione macchina, accelerando drasticamente gli algoritmi di intersezione.
    \item \textbf{Calcolo Parallelo e Sharding Distribuito:} Gli indici che superano la capacit\`a di un singolo nodo vengono frammentati (\textit{sharded}), tipicamente per partizionamento documentale (\textit{Document Sharding}), consentendo a cluster di servire migliaia di query parallele aggregando le risposte mediante broker centrali.
\end{itemize}

\FloatBarrier
\section{Architettura di un Sistema di Information Retrieval}

Come illustrato nello schema di Figura~\ref{fig:ir-architecture-detailed}, un sistema IR adotta una rigorosa separazione tra la \textbf{Pipeline Offline} (indicizzazione e addestramento) e la \textbf{Pipeline Online} (interrogazione a bassa latenza).

\begin{figure}[htbp]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    x=1cm,
    y=1cm,
    scale=0.85,
    box/.style={rectangle, rounded corners=3pt, draw=blue!75!black, fill=blue!8, thick, minimum width=2.5cm, minimum height=0.75cm, text width=2.4cm, align=center, font=\scriptsize},
    qbox/.style={rectangle, rounded corners=3pt, draw=blue!75!black, fill=blue!15, thick, minimum width=2.1cm, minimum height=0.65cm, text width=2.0cm, align=center, font=\scriptsize},
    db/.style={cylinder, shape border rotate=90, aspect=0.22, draw=cyan!75!black, fill=cyan!10, thick, minimum width=2.3cm, minimum height=0.9cm, text width=2.2cm, inner sep=2pt, align=center, font=\scriptsize\bfseries},
    serpbox/.style={rectangle, rounded corners=3pt, draw=teal!80!black, fill=teal!12, thick, minimum width=2.1cm, minimum height=0.75cm, text width=2.0cm, align=center, font=\scriptsize},
    arr/.style={-Stealth, thick, blue!80!black}
]
    % Dotted Boundary Line (Online / Offline)
    \draw[blue!40!black, dashed, thick] (-3.6, 2.8) -- (11.6, 2.8);
    \node[draw=blue!50!black, fill=blue!5, rounded corners=2pt, font=\bfseries\scriptsize, text=blue!80!black, inner sep=3pt, anchor=east] at (-2.0, 3.4) {ONLINE};
    \node[draw=blue!50!black, fill=blue!5, rounded corners=2pt, font=\bfseries\scriptsize, text=blue!80!black, inner sep=3pt, anchor=east] at (-2.0, 2.2) {OFFLINE};

    % ONLINE LAYER
    % Column 1: Query Pipeline
    \node[qbox] (query) at (0, 7.6) {Query};
    \node[qbox] (expquery) at (0, 6.3) {Expanded Query};
    \node[box] (qproc) at (0, 5.0) {Query Processing};

    % Column 2: Feature Lookup
    \node[box] (featlook) at (3.9, 5.0) {Feature Lookup\\and Computation};

    % Column 3: Learned Ranking
    \node[box] (rankfunc) at (7.8, 5.0) {Learned Ranking\\Function};
    \node[serpbox] (serp) at (11.0, 5.0) {\textbf{Ranked SERP}\\(Risultati)};

    % SHARED / BOUNDARY STORAGE (Cylinders)
    \node[db] (invidx) at (0, 2.8) {Inverted\\Index};
    \node[db] (docfeat) at (3.6, 2.8) {Document Features\\Repository};
    \node[db] (ltrmodel) at (7.2, 2.8) {Learning-to-Rank\\Model};

    % OFFLINE LAYER
    % Middle tier: Processors
    \node[box] (indexing) at (0, 0.8) {Indexing};
    \node[box] (featproc) at (3.6, 0.8) {Feature Processor};
    \node[box] (training) at (7.2, 0.8) {Training};

    % Bottom tier: Sources (Cylinders)
    \node[db] (doccoll) at (0, -1.2) {Document\\Collection};
    \node[db] (traindata) at (7.2, -1.2) {Training\\Data};

    % ARROWS - ONLINE
    \draw[arr] (query) -- (expquery);
    \draw[arr] (expquery) -- (qproc);
    \draw[arr] (invidx) -- (qproc);
    \draw[arr] (qproc) -- (featlook);
    \draw[arr] (docfeat) -- (featlook);
    \draw[arr] (featlook) -- (rankfunc);
    \draw[arr] (ltrmodel) -- (rankfunc);
    \draw[arr] (rankfunc) -- (serp);

    % ARROWS - OFFLINE
    \draw[arr] (doccoll) -- (indexing);
    \draw[arr] (indexing) -- (invidx);
    \draw[arr] (doccoll.north east) -- (featproc.south west);
    \draw[arr] (featproc) -- (docfeat);
    \draw[arr] (traindata) -- (training);
    \draw[arr] (training) -- (ltrmodel);
\end{tikzpicture}%
}
\caption{Architettura a griglia di un moderno motore di ricerca (pipeline Online vs. Offline).}
\label{fig:ir-architecture-detailed}
\end{figure}

\subsection{Pipeline di Text Processing}
Prima dell'indicizzazione, ogni flusso testuale attraversa quattro fasi sequenziali:
\begin{enumerate}
    \item \textbf{Tokenizzazione (\textit{Tokenization}):} Segmentazione della stringa di caratteri in sequenze di token lessicali, gestendo separatori, acronimi e punteggiatura.
    \item \textbf{Normalizzazione e Case Folding:} Conversione a caratteri minuscoli e rimozione o armonizzazione di diacritici (es. accenti) per garantire la coerenza tra query e documenti.
    \item \textbf{Filtraggio delle Stop Words:} Eliminazione di vocaboli ad altissima frequenza ma privi di valore semantico discriminante (articoli, preposizioni, congiunzioni).
    \item \textbf{Stemming e Lemmatizzazione:}
    \begin{itemize}
        \item \textit{Stemming:} Procedura algoritmica basata su regole euristiche (ad es. l'algoritmo di Porter~\cite{manning2008}) che tronca suffissi morfologici per estrarre la radice (\textit{stem}). Esempio: \texttt{"searching"}, \texttt{"searched"} $\to$ \texttt{"search"}.
        \item \textit{Lemmatizzazione:} Analisi linguistica formale che si avvale di dizionari morfologici per mappare ogni forma flessa al suo lemma canonico. Esempio: forme verbali \texttt{"sono"}, \texttt{"fui"} $\to$ lemma \texttt{"essere"}.
    \end{itemize}
\end{enumerate}

\FloatBarrier
\section{Propriet\`a del Linguaggio e Modelli di Reperimento}

La distribuzione delle parole nei testi naturali segue leggi empiriche che condizionano sia la progettazione degli indici sia le formule di ranking:
\begin{itemize}
    \item \textbf{Legge di Zipf:} La frequenza $f(r)$ di un termine \`e inversamente proporzionale al suo rango $r$ nella graduatoria di frequenza:
    \begin{equation}
    f(r) \propto \frac{1}{r}
    \end{equation}
    Poche parole frequentissime occupano la maggioranza delle occorrenze, mentre la stragrande maggioranza dei termini compare pochissime volte (\textit{long tail}).
    \item \textbf{Legge di Heaps:} La dimensione del vocabolario $|V|$ cresce come legge di potenza rispetto al numero totale di parole $N$ della collezione:
    \begin{equation}
    |V| = k \cdot N^\beta \quad (\text{con } 0.4 \le \beta \le 0.6)
    \end{equation}
    Il vocabolario non si satura mai completamente all'aumentare dei documenti.
\end{itemize}

I principali modelli di reperimento includono:
\begin{enumerate}
    \item \textbf{Modello Booleano:} Match deterministico basato su operatori logici ($\land, \lor, \neg$), privo di punteggio intrinseco di rilevanza.
    \item \textbf{Vector Space Model (TF-IDF):} Rappresentazione di query e documenti come vettori in uno spazio dimensionale $|V|$, pesati con frequenza del termine ($TF$) e frequenza inversa nei documenti ($IDF$). La rilevanza \`e calcolata tramite la similitudine del coseno.
    \item \textbf{Modello Probabilistico (BM25):} Standard de facto del reperimento lessicale basato sul principio probabilistico del 2-Poisson~\cite{buttcher2010}, che formalizza la saturazione asintotica della frequenza del termine e penalizza i documenti eccessivamente lunghi per evitare bias di verbosit\`a:
    \begin{equation}
    \operatorname{score}_{\text{BM25}}(q, d) = \sum_{t \in q} \operatorname{IDF}(t) \cdot \frac{tf(t, d) \cdot (k_1 + 1)}{tf(t, d) + k_1 \cdot \left(1 - b + b \cdot \dfrac{|d|}{\text{avgdl}}\right)}
    \label{eq:bm25-formula}
    \end{equation}
    dove:
    \begin{itemize}
        \item $\operatorname{IDF}(t) = \ln \left( \dfrac{N - df_t + 0.5}{df_t + 0.5} + 1 \right)$ quantifica la specificit\`a informativa del termine nell'intera collezione di $N$ documenti.
        \item $tf(t, d)$ \`e la frequenza grezza del termine $t$ all'interno del documento $d$.
        \item $k_1 \ge 0$ (parametro di saturazione del TF, tipicamente calibrato tra $1.2$ e $2.0$): controlla la rapidit\`a con cui il contributo di occorrenze ripetute dello stesso termine si appiattisce verso un valore asintotico.
        \item $b \in [0, 1]$ (parametro di penalizzazione della lunghezza, tipicamente $b \approx 0.75$): controlla l'intensit\`a della normalizzazione della lunghezza del documento $|d|$ rapportata alla lunghezza media $\text{avgdl}$ dei documenti della collezione. Quando $b=1$, la penalizzazione per documenti prolissi \`e massima; per $b=0$, la lunghezza \`e ininfluente.
    \end{itemize}
\end{enumerate}

\FloatBarrier
\section{L'Inverted Index (Indice Invertito)}

L'\textbf{Inverted Index} inverte l'orientamento logico della memorizzazione documentale, realizzando una mappatura $t \mapsto \mathcal{P}_t$, dove $\mathcal{P}_t$ \`e la lista ordinata dei documenti contenenti il termine $t$~\cite{manning2008}.

\begin{definition}[Inverted Index]
Un Inverted Index \`e costituito da due componenti:
\begin{enumerate}
    \item \textbf{Dizionario (\textit{Dictionary} o \textit{Vocabulary}):} Contiene l'insieme ordinato di tutti i termini unici presenti nella collezione, memorizzato con strutture di lookup rapido (B-Tree o Hash Table), insieme alla frequenza documentale $df_t$ e al puntatore all'inizio della lista.
    \item \textbf{Posting List:} Per ogni termine $t$, un array ordinato per identificatore di documento crescente ($\text{DocID}_1 < \text{DocID}_2 < \dots$). Ciascun elemento (posting) pu\`o includere metadati accessori quali frequenza locale del termine ($tf$) e offset posizionali all'interno del documento.
\end{enumerate}
\end{definition}

\begin{example}[Collezione Giocattolo di Riferimento]
Si consideri una collezione di tre documenti didattici:
\begin{itemize}
    \item $d_1$: \textit{"An inverted index is an index storing a mapping from terms to their occurrences in a set of documents."}
    \item $d_2$: \textit{"The inverted index data structure is a central component of a typical search engine indexing algorithm."}
    \item $d_3$: \textit{"The goal is to optimize the speed of the query: find the documents where a set of words occur."}
\end{itemize}
La Figura~\ref{fig:inverted-index-structure} illustra l'Inverted Index generato per i termini chiave estratti da questa collezione.
\end{example}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    scale=0.92,
    term/.style={rectangle, draw=blue!80!black, fill=blue!10, thick, minimum width=2.4cm, minimum height=0.62cm, font=\footnotesize\ttfamily, align=left},
    df/.style={rectangle, draw=blue!80!black, fill=blue!5, thick, minimum width=0.8cm, minimum height=0.62cm, font=\footnotesize, align=center},
    post/.style={rectangle, draw=purple!80!black, fill=purple!8, thick, minimum width=0.75cm, minimum height=0.62cm, font=\footnotesize\bfseries},
    ptr/.style={-Stealth, thick, blue!80!black},
    link/.style={-Stealth, thick, purple!80!black},
    header/.style={font=\bfseries\small, align=center}
]
    % Main Headers
    \node[header, blue!80!black] at (0.825, 4.2) {DIZIONARIO};
    \node[header, purple!80!black] at (5.3, 4.2) {POSTING LISTS};

    % Sub-headers
    \node[font=\footnotesize\itshape, blue!60!black] at (0, 3.6) {Termine};
    \node[font=\footnotesize\itshape, blue!60!black] at (1.65, 3.6) {$df_t$};
    \node[font=\footnotesize\itshape, purple!70!black] at (5.3, 3.6) {$\mathrm{DocID}_1 < \mathrm{DocID}_2 < \dots$};

    % Row 1: a -> 1, 2, 3
    \node[term] (t1) at (0, 2.9) {a};
    \node[df]   (df1) at (1.65, 2.9) {3};
    \node[post] (p11) at (4.0, 2.9) {1};
    \node[post] (p12) at (5.0, 2.9) {2};
    \node[post] (p13) at (6.0, 2.9) {3};

    % Row 2: an -> 1
    \node[term] (t2) at (0, 2.1) {an};
    \node[df]   (df2) at (1.65, 2.1) {1};
    \node[post] (p21) at (4.0, 2.1) {1};

    % Row 3: index -> 1, 2
    \node[term] (t3) at (0, 1.3) {index};
    \node[df]   (df3) at (1.65, 1.3) {2};
    \node[post] (p31) at (4.0, 1.3) {1};
    \node[post] (p32) at (5.0, 1.3) {2};

    % Row 4: inverted -> 1, 2
    \node[term] (t4) at (0, 0.5) {inverted};
    \node[df]   (df4) at (1.65, 0.5) {2};
    \node[post] (p41) at (4.0, 0.5) {1};
    \node[post] (p42) at (5.0, 0.5) {2};

    % Row 5: query -> 3
    \node[term] (t5) at (0, -0.3) {query};
    \node[df]   (df5) at (1.65, -0.3) {1};
    \node[post] (p51) at (4.0, -0.3) {3};

    % Arrows from Dictionary to Posting Lists
    \draw[ptr] (df1.east) -- (p11.west);
    \draw[ptr] (df2.east) -- (p21.west);
    \draw[ptr] (df3.east) -- (p31.west);
    \draw[ptr] (df4.east) -- (p41.west);
    \draw[ptr] (df5.east) -- (p51.west);

    % Links between postings
    \draw[link] (p11) -- (p12);
    \draw[link] (p12) -- (p13);
    \draw[link] (p31) -- (p32);
    \draw[link] (p41) -- (p42);
\end{tikzpicture}
\caption{Struttura di un Inverted Index per la collezione giocattolo ($d_1, d_2, d_3$): Dizionario dei termini con frequenza documentale $df_t$ e Posting Lists concatenate.}
\label{fig:inverted-index-structure}
\end{figure}

\subsection{Elaborazione delle Query e Algoritmo di Merge}
L'ordinamento intrinseco dei DocID consente di risolvere le interrogazioni booleane e per frase mediante scansioni coordinate delle posting list:
\begin{itemize}
    \item \textbf{Query Congiuntiva (AND):} Trovare i documenti contenenti contemporaneamente pi\`u termini. Ad esempio, per $q = \text{\texttt{"inverted" AND "index"}}$:
    \[
    \mathcal{P}_{\text{inverted}} \cap \mathcal{P}_{\text{index}} = [1, 2] \cap [1, 2] = [1, 2] \implies \{d_1, d_2\}
    \]
    Se invece si richiede $q = \text{\texttt{"query" AND "inverted" AND "index"}}$:
    \[
    (\mathcal{P}_{\text{inverted}} \cap \mathcal{P}_{\text{index}}) \cap \mathcal{P}_{\text{query}} = [1, 2] \cap [3] = \emptyset
    \]
    L'intersezione a due puntatori (Algoritmo~\ref{alg:merge-intersection-it}) garantisce complessit\`a $\BigO{|P_1| + |P_2|}$. Per ottimizzare query con $k > 2$ termini, la best practice consiste nell'elaborare le liste in ordine crescente di frequenza documentale $df_t$, partendo dalla posting list pi\`u corta per minimizzare le dimensioni intermedie.
    \item \textbf{Query Disgiuntiva (OR):} Trovare documenti contenenti almeno uno dei termini. Per $q = \text{\texttt{"query" OR "inverted"}}$:
    \[
    \mathcal{P}_{\text{query}} \cup \mathcal{P}_{\text{inverted}} = [3] \cup [1, 2] = [1, 2, 3] \implies \{d_1, d_2, d_3\}
    \]
    \item \textbf{Ricerca per Frase (\textit{Phrase Search}):} Per verificare la presenza di sequenze esatte di parole (es. \texttt{"inverted index"}), le posting list memorizzano per ogni DocID gli offset posizionali di comparsa. L'algoritmo verifica che:
    \[
    \text{pos}(\text{index}) - \text{pos}(\text{inverted}) = 1
    \]
    escludendo documenti in cui i termini compaiono in ordine invertito o a distanza arbitraria.
\end{itemize}

\begin{algorithm}[H]
\caption{Merge Intersection per Query Congiuntive ($P_1 \land P_2$)}
\label{alg:merge-intersection-it}
\KwInput{Due posting list ordinate $P_1$ e $P_2$}
\KwOutput{Lista ordinata $R$ dei documenti presenti in entrambe le liste}
$R \leftarrow \emptyset$\;
$i \leftarrow 1$; $j \leftarrow 1$\;
\While{$i \le |P_1|$ \textbf{e} $j \le |P_2|$}{
    \If{$P_1[i] == P_2[j]$}{
        $R.\text{append}(P_1[i])$\;
        $i \leftarrow i + 1$\;
        $j \leftarrow j + 1$\;
    }
    \ElseIf{$P_1[i] < P_2[j]$}{
        $i \leftarrow i + 1$\;
    }
    \Else{
        $j \leftarrow j + 1$\;
    }
}
\Return{$R$}\;
\end{algorithm}

\subsubsection{Dalla Teoria alla Pratica: Google Advanced Search}
I costrutti teorici dell'algebra booleana e del Phrase Search trovano immediata corrispondenza nell'interfaccia e nella sintassi operativa dei motori di ricerca commerciali (Tabella~\ref{tab:google-advanced-search}).

\begin{table}[htbp]
\centering
\small
\begin{tabularx}{\textwidth}{p{3.2cm} p{3.6cm} X}
\toprule
\textbf{Operatore Logico IR} & \textbf{Campo Google Advanced Search} & \textbf{Sintassi Operativa ed Esempio} \\
\midrule
\textbf{Congiunzione (AND)} & \textit{all these words} & Parole affiancate: \texttt{tri-colour rat terrier} \\
\addlinespace
\textbf{Ricerca per Frase} & \textit{this exact word or phrase} & Racchiusa tra doppi apici: \texttt{"rat terrier"} \\
\addlinespace
\textbf{Disgiunzione (OR)} & \textit{any of these words} & Operatore maiuscolo: \texttt{miniature OR standard} \\
\addlinespace
\textbf{Negazione (NOT)} & \textit{none of these words} & Prefisso meno senza spazi: \texttt{-rodent -"Jack Russell"} \\
\addlinespace
\textbf{Intervallo Numerico} & \textit{numbers ranging from \dots\ to} & Operatore due punti: \texttt{10..35 kg}, \texttt{\pounds 300..\pounds 500} \\
\bottomrule
\end{tabularx}
\caption{Mappatura diretta tra modelli formali di Query Processing e l'interfaccia di Ricerca Avanzata.}
\label{tab:google-advanced-search}
\end{table}

\FloatBarrier
\section{Tecniche di Compressione dell'Indice}

La compressione dell'Inverted Index risponde a una duplice esigenza: ridurre drasticamente l'occupazione di memoria secondaria e RAM, e convertire un collo di bottiglia di trasferimento I/O (lento) in un carico computazionale di decompressione da parte della CPU (estremamente veloce).

\subsection{Binary Interpolative Coding}
Il \textbf{Binary Interpolative Coding} (proposto da Moffat e Stuiver~\cite{moffat2000}) \`e una tecnica di compressione specificamente concepita per sequenze strettamente crescenti di interi (come i DocID nelle posting list).

\begin{definition}[Principio di Interpolazione]
Data una sottolista ordinata di $n$ interi $L = [x_1, x_2, \dots, x_n]$ nota per essere compresa nell'intervallo $[l, h]$ (ovvero $l \le x_1 < x_2 < \dots < x_n \le h$), si seleziona l'elemento centrale $m = \lfloor (n+1)/2 \rfloor$.
Poich\'e devono esistere almeno $m - 1$ valori distinti prima di $x_m$ e almeno $n - m$ valori distinti dopo $x_m$, il valore $x_m$ \`e strettamente vincolato nell'intervallo ristretto:
\begin{equation}
l + m - 1 \le x_m \le h - n + m
\end{equation}
Il numero di possibili valori distinti per $x_m$ \`e dato da:
\begin{equation}
R = (h - n + m) - (l + m - 1) + 1 = h - l - n + 1
\end{equation}
Il valore $(x_m - (l + m - 1))$ viene quindi codificato in binario utilizzando esattamente $\lceil \log_2 R \rceil$ bit. Successivamente, l'algoritmo si applica ricorsivamente alla met\`a sinistra rispetto a $[l, x_m - 1]$ e alla met\`a destra rispetto a $[x_m + 1, h]$.
\end{definition}

\begin{example}[Decomposizione Ricorsiva ad Albero]
Si consideri la posting list ordinata di $n = 12$ identificatori compresi nell'intervallo $[0, 62]$:
\[
L = [3, 4, 7, 13, 14, 15, 21, 25, 36, 38, 54, 62] \quad (n=12, m=6, l=0, h=62)
\]
L'albero di decomposizione (Figura~\ref{fig:interpolative-tree}) partiziona ricorsivamente la sequenza:
\begin{itemize}
    \item L'elemento centrale $x_6 = 15$ divide la sequenza nelle sottoliste $[3, 4, 7, 13, 14]$ con vincolo $(n=5, m=3, l=0, h=14)$ e $[21, 25, 36, 38, 54]$ con vincolo $(n=5, m=3, l=16, h=62)$.
    \item A livello inferiore, la prima sottolista si divide in $[3, 4]$ con parametri $(n=2, m=1, l=0, h=6)$ e $[13, 14]$ con parametri $(n=2, m=1, l=8, h=14)$.
    \item Le foglie codificano i singoli elementi: ad esempio per $[14]$ con parametri $(n=1, m=1, l=14, h=14)$, il range di valori ammissibili \`e:
    \[
    R = 14 - 14 - 1 + 1 = 1 \implies \lceil \log_2 1 \rceil = 0 \text{ bit!}
    \]
\end{itemize}
Quando i documenti sono densamente aggregati (\textit{clusterizzati}), il numero di valori ammissibili $R$ crolla verso $1$, portando il costo di memorizzazione a zero bit per molti elementi.
\end{example}

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    scale=0.78,
    transform shape,
    level distance=1.5cm,
    level 1/.style={sibling distance=8.5cm},
    level 2/.style={sibling distance=4.5cm},
    level 3/.style={sibling distance=2.4cm},
    node/.style={rectangle, rounded corners=3pt, draw=blue!80!black, fill=blue!8, thick, align=center, font=\scriptsize, inner sep=3pt},
    leaf/.style={rectangle, rounded corners=3pt, draw=teal!80!black, fill=teal!10, thick, align=center, font=\scriptsize\bfseries, inner sep=3pt},
    arr/.style={-Stealth, thick, blue!80!black}
]
    % Root
    \node[node] (root) {$[3,4,7,13,14,15,21,25,36,38,54,62]$\\[2pt]\tiny $(n=12, m=6, l=0, h=62)$}
        child {
            node[node] (l1) {$[3,4,7,13,14]$\\[2pt]\tiny $(n=5, m=3, l=0, h=14)$}
            child {
                node[node] (l11) {$[3,4]$\\[2pt]\tiny $(n=2, m=1, l=0, h=6)$}
                child {
                    node[leaf] (leaf1) {$[4]$\\[2pt]\tiny $(n=1, m=1, l=4, h=6)$}
                    edge from parent[arr]
                }
                edge from parent[arr]
            }
            child {
                node[node] (l12) {$[13,14]$\\[2pt]\tiny $(n=2, m=1, l=8, h=14)$}
                child {
                    node[leaf] (leaf2) {$[14]$\\[2pt]\tiny $(n=1, m=1, l=14, h=14)$\\[1pt]\tiny \color{teal!80!black}\textbf{0 bit!}}
                    edge from parent[arr]
                }
                edge from parent[arr]
            }
            edge from parent[arr]
        }
        child {
            node[node] (r1) {$[21,25,36,38,54]$\\[2pt]\tiny $(n=5, m=3, l=16, h=62)$}
            child {
                node[node] (r11) {$[21,25]$\\[2pt]\tiny $(n=2, m=1, l=16, h=35)$}
                child {
                    node[leaf] (leaf3) {$[25]$\\[2pt]\tiny $(n=1, m=1, l=22, h=35)$}
                    edge from parent[arr]
                }
                edge from parent[arr]
            }
            child {
                node[node] (r12) {$[38,54]$\\[2pt]\tiny $(n=2, m=1, l=37, h=62)$}
                child {
                    node[leaf] (leaf4) {$[54]$\\[2pt]\tiny $(n=1, m=1, l=39, h=62)$}
                    edge from parent[arr]
                }
                edge from parent[arr]
            }
            edge from parent[arr]
        };
\end{tikzpicture}
\caption{Albero di decomposizione dicotomica del Binary Interpolative Coding.}
\label{fig:interpolative-tree}
\end{figure}

\subsection{Compressione Basata su Dizionario: LZ77}
Per testi generali o stream di token, l'algoritmo \textbf{Lempel-Ziv '77} (LZ77) comprime la sequenza tramite una finestra scorrevole divisa in un buffer di ricerca (\textit{Search Buffer}) e un buffer di anteprima (\textit{Lookahead Buffer}), emettendo triple formattate come $(\text{offset}, \text{length}, \text{next\_char})$.

Si consideri la suddivisione analitica della sequenza testuale:
\[
T = \underbrace{\text{\texttt{a}}}_{(0, \text{'a'})} \; \underbrace{\text{\texttt{b}}}_{(0, \text{'b'})} \; \underbrace{\text{\texttt{a b a}}}_{(2, 3)} \; \underbrace{\text{\texttt{a a a}}}_{(1, 3)} \; \underbrace{\text{\texttt{b a b a a a a b}}}_{(7, 8)}
\]
La successione di puntatori generata \`e:
\[
(0, \text{\texttt{'a'}}), \; (0, \text{\texttt{'b'}}), \; (2, 3), \; (1, 3), \; (7, 8)
\]
In tal modo, lunghe ripetizioni lessicali sono rimpiazzate da compatti offset all'indietro nella finestra di scorrimento. Questa famiglia di algoritmi costituisce la spina dorsale di strumenti ampiamente adottati come \texttt{gzip}, \texttt{7-Zip} e \texttt{LZ4} per l'archiviazione compressa ad alto throughput.

\FloatBarrier
\section{Learning-to-Rank (LTR) e la Sfida della Non-Differenziabilit\`a}

All'interno dell'architettura di un motore di ricerca moderno (cfr. Figura~\ref{fig:ir-architecture-detailed}), i candidati ad alta rilevanza selezionati dall'Inverted Index vengono inoltrati alla fase di classificazione e riordino gestita da modelli di Machine Learning (\textbf{Learning-to-Rank}, LTR)~\cite{burges2010}.

\subsection{Formulazione del Problema di Ranking}
Nel paradigma LTR supervisionato, il dataset di addestramento \`e costituito da un insieme di query, a ciascuna delle quali \`e associata una lista di documenti valutati da annotatori umani o ricavati da segnali di log:
\begin{equation}
\mathcal{T} = \left\{ \left( q, \langle (d_1, y_1), (d_2, y_2), \dots, (d_m, y_m) \rangle \right) \right\}
\end{equation}
dove $y_i \in \{0, 1, 2, 3, 4\}$ quantifica il grado di rilevanza su scala multi-livello:
\begin{itemize}
    \item $y = 0$: Irrilevante (\textit{Irrelevant});
    \item $y = 1$: Marginalmente rilevante;
    \item $y = 2$: Rilevante;
    \item $y = 3$: Molto rilevante;
    \item $y = 4$: Perfettamente rilevante (\textit{Perfectly relevant}).
\end{itemize}

A partire da un vettore di feature estratto per ciascuna coppia query-documento $\vec{x}_{q, d} = \Phi(q, d)$, un modello di Machine Learning (spesso assimilato a una complessa funzione "black box") calcola uno score di rilevanza $s_i = f_\theta(\vec{x}_{q, d_i})$. La lista ordinata finale (\textit{ranked list}) restituita all'utente viene generata ordinando i documenti per punteggio decrescente:
\begin{equation}
\pi = \operatorname{sort}_{\downarrow} \left( \{ (d_i, s_i) \}_{i=1}^m \right)
\end{equation}

\begin{figure}[htbp]
\centering
\resizebox{\linewidth}{!}{%
\begin{tikzpicture}[
    box/.style={rectangle, rounded corners=3pt, draw=blue!80!black, fill=blue!8, thick, minimum width=2.4cm, minimum height=0.8cm, align=center, font=\scriptsize},
    model/.style={rectangle, rounded corners=3pt, draw=purple!80!black, fill=purple!12, thick, minimum width=2.8cm, minimum height=0.9cm, align=center, font=\scriptsize\bfseries},
    blackbox/.style={rectangle, rounded corners=3pt, draw=black!90, fill=black!85, text=white, thick, minimum width=2.6cm, minimum height=0.85cm, align=center, font=\scriptsize\bfseries},
    data/.style={rectangle, rounded corners=3pt, draw=gray!70!black, fill=gray!8, dashed, thick, align=left, font=\tiny, inner sep=4pt},
    arr/.style={-Stealth, thick, blue!80!black}
]
    \draw[gray!60!black, dashed, thick] (-1.5, 1.15) -- (9.5, 1.15);
    \node[font=\scriptsize\bfseries, text=gray!70!black, anchor=west] at (-1.5, 1.35) {ADDESTRAMENTO};
    \node[font=\scriptsize\bfseries, text=gray!70!black, anchor=west] at (-1.5, -0.35) {INFERENZA ONLINE};

    % Training stage
    \node[data] (train) at (0, 2.4) {\textbf{Dataset di addestramento: query e giudizi}\\[2pt]
        $q_a, \langle (d_1, y_{a1}), (d_2, y_{a2}), (d_3, y_{a3}), (d_5, y_{a5}), \dots \rangle$\\[1pt]
        $q_b, \langle (d_{99}, y_{b99}), (d_{98}, y_{b98}), (d_{97}, y_{b97}), \dots \rangle$\\[2pt]
        \textit{Label graduata $y \in \{0, \dots, 4\}$ (da irrilevante a perfetto)}};
    
    \node[model] (ml) at (7.2, 2.4) {Machine Learning Model\\[2pt]\tiny (Ottimizzazione della Graduatoria)};

    % Online inference stage
    \node[box] (inonline) at (0, 0) {\textbf{Query $q$ online}\\[2pt]Documenti candidati $\{d_1, d_2, d_3, \dots\}$};
    \node[blackbox] (bbox) at (4.4, 0) {Ranking Function\\[2pt]\tiny (Modello Appreso)};
    \node[box, draw=teal!80!black, fill=teal!10] (ranked) at (8.8, 0) {\textbf{Ranked SERP Finale}\\[2pt]$d_{17}, d_{13}, d_{666}, \dots$};

    \draw[arr] (train) -- (ml);
    \draw[arr] (ml.south) -- ++(0,-0.55) -| (bbox.north);
    \draw[arr] (inonline) -- (bbox);
    \draw[arr] (bbox) -- (ranked);
\end{tikzpicture}
}%
\caption{Architettura concettuale del Learning-to-Rank (LTR): dall'addestramento con label graduate all'ordinamento della SERP online.}
\label{fig:learning-to-rank-flow}
\end{figure}

\subsection{La Sfida Teorica: La Non-Differenziabilit\`a del Ranking}
In un problema di regressione convenzionale, l'algoritmo minimizza l'errore quadratico medio $\sum_i (f_\theta(x_i) - y_i)^2$. Nell'Information Retrieval, tuttavia, vige un principio cardine:
\begin{alertblock}{Il Cardine dell'LTR}
\textbf{The goal is to learn the ranking, not the label}: l'obiettivo primario non \`e stimare accuratamente il valore numerico isolato $y$, bens\`i determinare l'ordinamento relativo ottimale dei documenti nella lista presentata all'utente, massimizzando metriche cumulative quali l'\textbf{NDCG} (\textit{Normalized Discounted Cumulative Gain}) o la \textbf{MAP} (\textit{Mean Average Precision}).
\end{alertblock}

Questa formulazione introduce una difficolt\`a matematica sostanziale:
\begin{itemize}
    \item Le metriche di ranking $\mathcal{M}(\pi)$ sono basate sul \textbf{rango discreto} (la posizione $1, 2, \dots, k$) assegnato a ciascun documento dalla funzione di ordinamento.
    \item L'operazione di ordinamento ($\operatorname{sort}$) \`e una funzione discontinua, a gradini e costante a tratti.
    \item Variando in modo continuo i parametri $\theta$ del modello, le posizioni dei documenti rimangono inalterate (gradiente esattamente nullo) fino al punto in cui due punteggi si scambiano, determinando un salto discontinuo (gradiente non definito):
    \begin{equation}
    \frac{\partial \mathcal{M}_{\text{NDCG}}(\pi_\theta)}{\partial \theta} = 0 \quad \text{quasi ovunque (a.e.)}
    \end{equation}
    \item Di conseguenza, \textbf{non \`e possibile applicare direttamente la discesa del gradiente stocastico (SGD) o la retropropagazione convenzionale} sulle metriche di ranking ufficiali!
\end{itemize}

\subsection{Famiglie di Modelli LTR e Gradienti Surrogati}
Per aggirare la non-differenziabilit\`a del ranking, la letteratura ha sviluppato tre metodologie principali:
\begin{enumerate}
    \item \textbf{Approccio Pointwise:} Tratta ciascun documento come un'istanza indipendente, trasformando il problema in una regressione su $y$ o in una classificazione ordinata. Pur consentendo l'uso immediato di SGD, questo approccio ignora la natura intrinsecamente comparativa e competitiva della graduatoria.
    \item \textbf{Approccio Pairwise (es. RankNet):} Considera coppie di documenti $(d_i, d_j)$ per la stessa query aventi differente rilevanza ($y_i > y_j$). L'obiettivo diviene minimizzare il numero di inversioni, modellando la probabilit\`a che $d_i$ preceda $d_j$ tramite una sigmoide differenziabile:
    \[
    P(d_i \succ d_j) = \frac{1}{1 + e^{-\sigma (s_i - s_j)}}
    \]
    La cross-entropy su queste probabilit\`a \`e liscia e differenziabile.
    \item \textbf{Approccio Listwise e Gradienti $\lambda$ (LambdaMART):} Ottimizza l'intera lista. L'algoritmo \textbf{LambdaRank} introduce gradienti virtuali surrogati ($\lambda_{ij}$), definiti pesando il gradiente pairwise con la variazione effettiva della metrica $|\Delta \operatorname{NDCG}_{ij}|$ derivante dallo scambio di posizione tra $d_i$ e $d_j$:
    \begin{equation}
    \lambda_{ij} = \frac{-\sigma}{1 + e^{\sigma(s_i - s_j)}} \cdot |\Delta \operatorname{NDCG}_{ij}|
    \end{equation}
    La combinazione di questi gradienti con alberi di decisione potenziati (\textit{Gradient Boosted Decision Trees}) ha dato origine a \textbf{LambdaMART}~\cite{burges2010}, per oltre un decennio lo standard di riferimento nei motori di ricerca commerciali.
\end{enumerate}

\FloatBarrier
\section{Evoluzione verso il Neural Information Retrieval}

A partire dal 2018, l'avvento dei modelli basati su architettura Transformer e di BERT ha inaugurato l'era del Neural Information Retrieval~\cite{khattab2020,formal2021}. I modelli Transformer apprendono la rappresentazione semantica profonda del testo tramite meccanismi di pre-addestramento auto-supervisionato su vasta scala, principalmente il \textbf{Masked Language Modeling (MLM)}: durante l'addestramento, una porzione casuale dei token (circa il $15\%$) viene mascherata tramite il token speciale \texttt{[MASK]}, e la rete impara a predirne l'identit\`a lessicale tramite un classificatore denso e una funzione softmax sul vocabolario $|V|$.

Nel reperimento documentale, gli approcci neurali si suddividono in due famiglie architetturali fondamentali (Figura~\ref{fig:cross-vs-bi-encoder}):

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
    box/.style={rectangle, rounded corners=4pt, draw=blue!80!black, fill=blue!8, thick, minimum width=2.4cm, minimum height=0.8cm, inner sep=4pt, align=center, font=\small},
    proc/.style={rectangle, rounded corners=4pt, draw=teal!80!black, fill=teal!8, thick, minimum width=2.5cm, minimum height=1.15cm, inner sep=6pt, align=center, font=\small},
    score/.style={rectangle, rounded corners=4pt, draw=purple!80!black, fill=purple!10, thick, minimum width=2.8cm, minimum height=0.85cm, inner sep=5pt, align=center, font=\small\bfseries},
    arr/.style={-Stealth, thick, blue!80!black},
    title/.style={font=\bfseries\normalsize, align=center}
]
    % Divider
    \draw[gray!35, dashed, thick] (6.6, 5.0) -- (6.6, -0.5);

    % --- INTERACTION-BASED (CROSS-ENCODER) ---
    \node[title, blue!80!black] at (3.0, 4.4) {Cross-Encoder\\[2pt]\footnotesize (Interazione)};

    \node[box] (q1) at (1.6, 3.2) {Query $q$};
    \node[box] (d1) at (4.4, 3.2) {Document $d$};
    \node[proc, minimum width=5.2cm] (cross) at (3.0, 1.7) {Cross-Encoder (BERT)\\[3pt]\footnotesize Full Cross-Attention su tutti i token};
    \node[score] (s1) at (3.0, 0.1) {Score $s(q, d)$};

    \draw[arr] (q1.south) -- (cross.140);
    \draw[arr] (d1.south) -- (cross.40);
    \draw[arr] (cross.south) -- (s1.north);

    % --- REPRESENTATION-BASED (BI-ENCODER) ---
    \node[title, teal!80!black] at (10.2, 4.4) {Bi-Encoder\\[2pt]\footnotesize (Rappresentazione)};

    \node[box] (q2) at (8.8, 3.2) {Query $q$};
    \node[box] (d2) at (11.6, 3.2) {Document $d$};
    \node[proc, minimum width=2.5cm] (encq) at (8.8, 1.7) {Query Encoder\\[3pt]\footnotesize $\vec{e}_q = f_\theta(q)$};
    \node[proc, minimum width=2.5cm] (encd) at (11.6, 1.7) {Doc Encoder\\[3pt]\footnotesize $\vec{e}_d = g_\theta(d)$};
    \node[score] (s2) at (10.2, 0.1) {Similarity $\vec{e}_q \cdot \vec{e}_d$};

    \draw[arr] (q2.south) -- (encq.north);
    \draw[arr] (d2.south) -- (encd.north);
    \draw[arr] (encq.south) -- (s2.140);
    \draw[arr] (encd.south) -- (s2.40);
\end{tikzpicture}
\caption{Confronto strutturale tra Cross-Encoder (Interazione) e Bi-Encoder (Rappresentazione).}
\label{fig:cross-vs-bi-encoder}
\end{figure}

\begin{enumerate}
    \item \textbf{Modelli Basati sull'Interazione (\textit{Cross-Encoders}):} La query e il documento vengono concatenati in un unico input:
    \[
    \text{Input} = \texttt{[CLS]} \circ q \circ \texttt{[SEP]} \circ d
    \]
    ed elaborati congiuntamente attraverso tutti i livelli di self-attention del Transformer. Catturano interazioni contestuali profonde tra ciascuna coppia di token, offrendo la massima efficacia predittiva. Tuttavia, il costo computazionale proibitivo ($\BigO{|\mathcal{D}| \times \text{Transformer}}$) ne preclude l'uso su larga scala; vengono impiegati esclusivamente come re-ranker di secondo livello sui top-100 candidati.
    \item \textbf{Modelli Basati sulla Rappresentazione (\textit{Bi-Encoders}):} La query e il documento vengono proiettati in modo disaccoppiato in uno spazio vettoriale mediante due encoder distinti. I vettori dei documenti possono essere pre-calcolati offline e indicizzati, riducendo il matching online a un prodotto scalare o a una ricerca di vicini approssimati.
\end{enumerate}

\subsection{Le Tre Famiglie di Rappresentazioni Vettoriali}
Nel contesto dei modelli a rappresentazione, si distinguono tre tipologie di embedding:
\begin{enumerate}
    \item \textbf{Dense Single-Vector (es. DPR, Contriever):} Ogni documento e ogni query sono sintetizzati in un unico vettore denso a $d$ dimensioni (ad es. $d = 768$). L'interrogazione richiede indici vettoriali k-ANN su grafi (come HNSW).
    \item \textbf{Dense Multi-Vector (es. ColBERT~\cite{khattab2020}):} Viene generato un vettore denso per ciascun token del documento e della query. La rilevanza \`e determinata tramite il meccanismo di \textbf{Late Interaction} con operatore \textit{MaxSim}:
    \begin{equation}
    S(q, d) = \sum_{t_q \in q} \max_{t_d \in d} \left( \vec{E}_{t_q} \cdot \vec{E}_{t_d}^T \right)
    \end{equation}
    Preserva l'identit\`a dei singoli termini offrendo un'eccellente generalizzazione, a fronte di un costo di memorizzazione pi\`u consistente.
    \item \textbf{Sparse Single-Vector (es. SPLADE~\cite{formal2021}):} I documenti e le query sono codificati in vettori sparsi ad altissima dimensione mappati direttamente sui token del vocabolario $|V|$ ($|V| \approx 30\,000$). I coefficienti numerici indicano il peso d'importanza appreso e includono l'espansione automatica di sinonimi.
    \begin{itemize}
        \item \textbf{Vantaggi:} Altissima efficacia sia nelle collezioni di dominio (\textit{in-domain}) sia nei test a trasferimento zero-shot (\textit{out-of-domain}).
        \item \textbf{Efficienza:} Pi\`u compatti e rapidi rispetto ai modelli multi-vettore, possono essere memorizzati direttamente all'interno delle posting list di un classico Inverted Index.
        \item \textbf{Interpretabilit\`a:} I pesi non nulli corrispondono a parole comprensibili dall'uomo, facilitando il debug e la spiegabilit\`a.
    \end{itemize}
\end{enumerate}

\begin{example}[Espansione Semantica di SPLADE]
Si consideri la query in linguaggio naturale:
\[
q = \text{\texttt{"what shoes do most nba players wear"}}
\]
Un modello neurale a vettori sparsi (come SPLADE~\cite{formal2021}) attiva un sottoinsieme selezionato di componenti lessicali sul vocabolario $|V|$, producendo un vettore con pesi appresi:
\begin{center}
\small\ttfamily
nba: 2.56, \quad shoes: 2.32, \quad shoe: 2.14, \quad basketball: 1.97, \\
wearing: 1.55, \quad wear: 1.52, \quad most: 1.34, \quad players: 1.25
\end{center}
Questo esempio illustra le due propriet\`a cardine dei modelli sparsi neurali:
\begin{enumerate}
    \item \textbf{Espansione Semantica dei Termini (\textit{Vocabulary Expansion}):} Il modello inserisce autonomamente con peso elevato il termine \texttt{"basketball"} ($1.97$), bench\'e totalmente assente nel testo originario della query, risolvendo il problema del disallineamento lessicale (\textit{vocabulary mismatch}).
    \item \textbf{Armonizzazione Morfologica:} Attribuisce pesi coordinati a singolari/plurali (\texttt{shoe} / \texttt{shoes}) e forme flesse verbali (\texttt{wear} / \texttt{wearing}), combinando i vantaggi dello stemming linguistico e del deep learning.
\end{enumerate}
\end{example}

\FloatBarrier
\section{\texorpdfstring{Ricerca dei Vicini Approssimati ($k$-ANN) e Scalabilit\`a}{Ricerca dei Vicini Approssimati (k-ANN) e Scalabilit\`a}}

Nei sistemi di reperimento neurale a rappresentazione vettoriale, l'interrogazione online coincide con il problema del \textbf{Maximum Inner Product Search (MIPS)}: individuare i primi $k$ documenti della collezione $\mathcal{D}$ che massimizzano il prodotto scalare con il vettore della query $\vec{q}$:
\begin{equation}
\operatorname{argmax}_{v \in \mathcal{D}}^{(k)} \; \vec{q}^{\,T} \cdot \vec{v}
\end{equation}

Nelle collezioni su scala Web moderna (ad es. il benchmark standard MS MARCO):
\begin{itemize}
    \item La collezione comprende oltre $8.8$ milioni di documenti ($|\mathcal{D}| \approx 8.8 \times 10^6$).
    \item Lo spazio vettoriale lessicale include $|V| \approx 30\,000$ componenti distinte.
    \item La sparsit\`a \`e marcata: ciascun documento presenta in media solo circa $150$ componenti con coefficiente non nullo.
\end{itemize}

\subsection{Forward Index e il Framework TusKANNy}
Per evitare il costo computazionale proibitivo della scansione esaustiva lineare, i sistemi $k$-ANN sparsi memorizzano la collezione mediante una combinazione di indici invertiti e di un \textbf{Forward Index} (Indice Diretto). Nel Forward Index, ciascun documento \`e compresso come una sequenza contigua di coppie (componente, valore):
\begin{equation}
d_i = \langle (c_1, v_1), (c_2, v_2), \dots, (c_m, v_m) \rangle \quad (\text{con } m \approx 150)
\end{equation}
In fase di query ($q = \langle (c_1, v_1), \dots, (c_p, v_p) \rangle$), l'algoritmo calcola il prodotto scalare selettivo solo sulle componenti condivise.

Queste tecniche sono state protagoniste della competizione internazionale \textbf{NeurIPS'23 Big-ANN Challenge} e del benchmark \textbf{SISAP 2026}. In tale contesto si colloca \textbf{TusKANNy} (\url{https://tuskanny.com/}), una libreria ad altissime prestazioni interamente scritta in Rust sviluppata congiuntamente da ricercatori dell'ISTI-CNR e dell'Universit\`a di Pisa. TusKANNy implementa algoritmi avanzati di scansione vettoriale (come \textit{Seismic} e \textit{Kannolo}) capaci di servire oltre $15\,000$ query al secondo (QPS) con una precisione di richiamo (\textit{recall}) superiore al $90\%$, operando con latenze sub-millisecondo.
